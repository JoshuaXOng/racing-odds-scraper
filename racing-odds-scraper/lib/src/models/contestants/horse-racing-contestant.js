import { Contestant } from "./contestant";
export class HorseRacingContestant extends Contestant {
    constructor({ id, name, horse }) {
        super({ id, name });
        this.horse = horse;
    }
}
//# sourceMappingURL=horse-racing-contestant.js.map