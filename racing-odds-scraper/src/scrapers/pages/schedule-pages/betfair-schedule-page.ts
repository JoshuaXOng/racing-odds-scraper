import { SchedulePage } from "../schedule-page";

export class BetfairSchedulePage extends SchedulePage {
  constructor(url: URL) {
    super(url);
  }

  venuesName() {
    
  };

  venuesToEventsMap() {

  };
}